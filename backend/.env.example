DATABASE_URL=postgresql+asyncpg://postgres:Danil228@localhost:5440/english_app
SECRET_KEY=change-me-in-production-use-long-random-string
# Android OAuth: package name com.english.english_words; add SHA-1 in Cloud Console (see README).

# Web client: used by backend (OAuth + id_token verification) and by app as serverClientId for native sign-in
GOOGLE_CLIENT_ID=api-key
GOOGLE_CLIENT_SECRET=api-key
# Android client: created in Cloud Console (package + SHA-1). Not used by backend; app uses Web client ID above.
GOOGLE_ANDROID_CLIENT_ID=api-key
REDIRECT_URI=http://localhost:8000/auth/google/callback
GEMINI_API_KEY=your-gemini-api-key